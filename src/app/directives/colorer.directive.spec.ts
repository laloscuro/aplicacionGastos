import { ColorerDirective } from './colorer.directive';

describe('ColorerDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorerDirective();
    expect(directive).toBeTruthy();
  });
});
